/**
 * 
 * @param {string} string1 
 * @param {string} string2 
 * @returns {boolean} 
 * 
 * str1 = "abab" str2 = "xyxy"
 * A string is isomopphic if every character in string 1 mapped to str2 and viceversa;
 */
const isIsomorphic = (string1, string2) => {
    if (string1.length !== string2) return false;
    const count = new Map();

    return true;    
}
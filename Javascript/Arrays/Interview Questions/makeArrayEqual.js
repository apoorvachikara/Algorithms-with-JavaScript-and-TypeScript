/**
 * 
 * @param {*} array1 
 * @param {*} array2 
 * Find sub-arrays from given two arrays such that they have equal sum
 * Count subarrays which have elements less than or equal to X
 * 
 * In how many steps, can you make the array1 and array2 equal.
 * array1 = [1, 2, 6, -1, 0, -4];
 * array2 = [-4, 0, 6, -1, ];
 */
const makeEqual = (array1, array2) => {

}